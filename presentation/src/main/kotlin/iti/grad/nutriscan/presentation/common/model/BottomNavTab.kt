package iti.grad.nutriscan.presentation.common.model

/**
 * Bottom navigation tab identifiers shared by every screen showing [iti.grad.nutriscan.presentation.common.components.AppBottomNavBar].
 */
enum class BottomNavTab {
    HOME,
    CALORIES,
    SCAN,
    SHOPPING,
    PROFILE,
}
