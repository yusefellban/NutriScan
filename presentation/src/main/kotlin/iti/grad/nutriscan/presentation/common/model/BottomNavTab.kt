package iti.grad.nutriscan.presentation.common.model

import iti.grad.nutriscan.presentation.common.components.AppBottomNavBar

/**
 * Bottom navigation tab identifiers shared by every screen showing [AppBottomNavBar].
 */
enum class BottomNavTab {
    HOME,
    CALORIES,
    SCAN,
    SAVED,
    PROFILE,
}
