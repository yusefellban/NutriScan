package iti.grad.nutriscan.presentation.common.model

/**
 * Represents the health verdict of a scanned product.
 * Used by UI components like ProductCard.
 */
enum class ProductVerdict {
    SAFE,
    CAUTION,
    UNSAFE
}
