package iti.grad.nutriscan.presentation.home.state

/**
 * Bottom navigation tab identifiers for the Home screen.
 */
enum class BottomNavTab {
    HOME,
    HISTORY,
    SCAN,
    SHOPPING,
    PROFILE,
}
